version: "3.9"

services:
  todo:
    build: .
    container_name: todo
    ports:
      - "8080:80"
    volumes:
      - todo_data:/data
      - ./frontend:/usr/share/nginx/html
    restart: unless-stopped

volumes:
  todo_data:
